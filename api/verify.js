const Stripe=require('stripe');module.exports=async function(req,res){if(!process.env.STRIPE_SECRET_KEY)return res.status(503).json({paid:false});try{const stripe=new Stripe(process.env.STRIPE_SECRET_KEY),s=await stripe.checkout.sessions.retrieve(req.query.session_id);res.status(200).json({paid:s.payment_status==='paid'&&s.mode==='payment'&&s.amount_total===100})}catch(e){res.status(400).json({paid:false})}};