const Stripe=require('stripe');module.exports=async function(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(!process.env.STRIPE_SECRET_KEY)return res.status(503).json({error:'Stripe not connected'});try{const stripe=new Stripe(process.env.STRIPE_SECRET_KEY);const origin=process.env.SITE_URL||`${req.headers['x-forwarded-proto']||'https'}://${req.headers.host}`;const s=await stripe.checkout.sessions.create({mode:'payment',line_items:[{price_data:{currency:'usd',unit_amount:100,product_data:{name:'OneBuckQR — Permanent Static QR Code',description:'One-time purchase. PNG + SVG. No subscription.'}},quantity:1}],success_url:`${origin}/?paid=1&session_id={CHECKOUT_SESSION_ID}`,cancel_url:`${origin}/`,submit_type:'pay'});res.status(200).json({url:s.url})}catch(e){console.error(e);res.status(500).json({error:'Could not start checkout.'})}};